jdk:
  - openjdk11
before_install:
  - ./scripts/prepareJitpackEnvironment.sh

install:
  - echo "Installing"
  - ./gradlew clean install -x test # Note: If you want to run tests, remove `-x test`

build:
  - ./gradlew test spotlessApply build